spring:
  application:
    name: discodeit
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB


server:
  port: 8081

discodeit:
  repository:
    type: file   # jcf | file
    file-directory: .discodeit/

