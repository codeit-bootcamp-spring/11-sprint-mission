spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/discodeit_test
    username: discodeit_user
    password: discodeit1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true

discodeit:
  storage:
    type: local
    local:
      root-path: .discodeit/storage