server:
  port: 80

spring:
  datasource:
    driver-calss-name: org.postgresql.Driver
    url: jdbc:postgresql://[운영DB주소]:5432/discodeit
    username: prod_user
    password: prod_password

logging:
  level:
    com.sprint.mission.discodeit: info
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace